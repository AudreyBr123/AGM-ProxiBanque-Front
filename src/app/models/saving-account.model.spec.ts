import { SavingAccountModel } from './saving-account.model';

describe('SavingAccountModel', () => {
  it('should create an instance', () => {
    expect(new SavingAccountModel()).toBeTruthy();
  });
});
