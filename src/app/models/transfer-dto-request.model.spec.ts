import { TransferDtoRequest } from './transfer-dto-request.model';

describe('TransferDtoRequest', () => {
  it('should create an instance', () => {
    expect(new TransferDtoRequest()).toBeTruthy();
  });
});
