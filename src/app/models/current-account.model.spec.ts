import { CurrentAccountModel } from './current-account.model';

describe('CurrentAccountModel', () => {
  it('should create an instance', () => {
    expect(new CurrentAccountModel()).toBeTruthy();
  });
});
